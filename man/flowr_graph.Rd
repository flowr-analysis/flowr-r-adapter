% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph.R
\name{flowr_graph}
\alias{flowr_graph}
\title{Turn a flowR graph into vertex and edge data frames}
\usage{
flowr_graph(
  code = NULL,
  file = NULL,
  folder = NULL,
  type = c("dataflow", "call-graph"),
  session = NULL
)
}
\arguments{
\item{code}{A string of R source code. Provide this, \code{file} or \code{folder}.}

\item{file}{Path to a single R file to analyse.}

\item{folder}{Path to a folder; all \code{.R} files in it (recursively) are
analysed together for a multi-file slice. Needs a real engine.}

\item{type}{\code{"dataflow"} (default) or \code{"call-graph"}.}

\item{session}{A \code{flowr_session} (from \code{\link[=flowr_connect]{flowr_connect()}}); the central default
session is used when \code{NULL}. Pass an explicit one to work with several at once.}
}
\value{
A \code{flowr_graph}: a list of \code{vertices} and \code{edges} data frames.
}
\description{
\strong{Experimental.} Runs the \code{dataflow} (or \code{call-graph}) query and returns the
graph as tidy \code{vertices} (\code{id}, \code{tag}) and \code{edges} (\code{from}, \code{to}, \code{type}) data
frames, ready for \code{\link[=flowr_as_igraph]{flowr_as_igraph()}} or any graph tool.
}
\seealso{
\code{\link[=flowr_as_igraph]{flowr_as_igraph()}}
}
