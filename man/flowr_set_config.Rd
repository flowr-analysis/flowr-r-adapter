% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{flowr_set_config}
\alias{flowr_set_config}
\title{Change flowr configuration values}
\usage{
flowr_set_config(..., persist = FALSE)
}
\arguments{
\item{...}{Named settings to change, e.g. \code{engine = "binary"}. Names must be
valid flowr options (see \code{\link[=flowr_config]{flowr_config()}}).}

\item{persist}{Also write the settings to the user config file
(\code{\link[=flowr_config_file]{flowr_config_file()}}), so they survive across sessions.}
}
\value{
The updated configuration (a \code{flowr_config}), invisibly.
}
\description{
Sets one or more configuration knobs for the current session (as R options)
and, optionally, persists them to the user JSON config file so they apply to
future sessions too. This is the convenient way to switch engine, flowR
version, timeouts and the like without editing files by hand.
}
\details{
Equivalent lower-level forms all work and are fully supported: set the R
option directly with \verb{options(flowr.<name> = value)}, the environment variable
\verb{FLOWR_<NAME>}, or assign into the configuration object,
\verb{cfg <- flowr_config(); cfg$engine <- "binary"} (which sets the option for
you).
}
\examples{
\dontrun{
flowr_set_config(engine = "binary", request_timeout = 300)
flowr_set_config(flowr_version = "2.12.0", persist = TRUE)
}
}
\seealso{
\code{\link[=flowr_config]{flowr_config()}}, \code{\link[=flowr_status]{flowr_status()}}
}
