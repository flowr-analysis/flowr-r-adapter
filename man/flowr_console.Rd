% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{flowr_console}
\alias{flowr_console}
\title{Open flowR's own interactive console}
\usage{
flowr_console(
  engine = flowr_option("engine"),
  flowr_version = flowr_option("flowr_version"),
  r_access = FALSE
)
}
\arguments{
\item{engine}{Which engine's flowR CLI to launch. Defaults to the configured
engine, resolving \code{auto} the same way a session would.}

\item{flowr_version}{flowR version to use.}

\item{r_access}{Allow the console to execute arbitrary R code (flowR's
\code{--r-session-access}). Since you are already in R, this is the easy way to
turn the flowR REPL into one that also runs R. It uses the r-shell engine
and, being an explicit opt-in, overrides secure mode for this console only.}
}
\value{
The flowR process exit status, invisibly.
}
\description{
Hands terminal control to flowR's native command-line REPL, with its own tab
completion, history and \code{:}-commands. Unlike \code{\link[=flowr_repl]{flowr_repl()}} (which relays one
line at a time over the socket), this launches flowR as a normal interactive
process with inherited stdin/stdout, so it is the full flowR shell. It returns
when you leave the flowR REPL (\verb{:quit}).
}
\details{
Available for the \code{binary}, \code{bundled} and \code{node} engines (the \code{docker} engine
has no terminal handoff; use \code{\link[=flowr_repl]{flowr_repl()}} there). Needs an interactive
terminal.
}
\examples{
\dontrun{
flowr_console()                # a full flowR shell with native completion
flowr_console(r_access = TRUE) # ... that can also execute R code
}
}
\seealso{
\code{\link[=flowr_repl]{flowr_repl()}}
}
