% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lint.R
\name{flowr_lint_fix}
\alias{flowr_lint_fix}
\title{Apply flowR's linter quick fixes}
\usage{
flowr_lint_fix(
  code = NULL,
  file = NULL,
  folder = NULL,
  session = NULL,
  rules = NULL,
  preview = FALSE
)
}
\arguments{
\item{code}{A string of R source, or a \code{\link[=flowr_lint]{flowr_lint()}} result to reuse (see
\emph{Details}). See \code{\link[=flowr_lint]{flowr_lint()}} for \code{file}/\code{folder}/\code{rules}.}

\item{file}{Path to a single R file to analyse.}

\item{folder}{Path to a folder; all \code{.R} files in it (recursively) are
analysed together for a multi-file slice. Needs a real engine.}

\item{session}{A \code{flowr_session} (from \code{\link[=flowr_connect]{flowr_connect()}}); the central default
session is used when \code{NULL}. Pass an explicit one to work with several at once.}

\item{rules}{Which linter rules to run, as a character vector of rule names
(e.g. \code{c("absolute-file-paths", "unused-definitions")}). \code{NULL} (default)
uses the active set from \code{options(flowr.lint_rules = )} /
\code{\link[=flowr_set_config]{flowr_set_config()}}, which itself defaults to flowR's full rule set --- the
same rules the flowR VS Code extension enables. See the \emph{Linting} section of
\code{vignette("flowr-engines")} for the rule list and configuration.}

\item{preview}{If \code{TRUE}, do not write files; return the fixed text instead
(a character vector named by file).}
}
\value{
Invisibly: the fixed code string for \code{code} input; for file/folder
input, a named integer of fixes applied per file, or --- when
\code{preview = TRUE} --- the fixed text of each file. Findings without a quick
fix are left untouched.
}
\description{
Applies the automatic fixes flowR attaches to some findings (currently
absolute file paths, unused definitions and naming conventions) --- the same
quick fixes the flowR VS Code extension offers. When linting \strong{files} or a
\strong{folder}, the fixes are written back to those files in place (use
\code{preview = TRUE} to get the fixed text without touching disk). When linting a
snippet via \code{code}, the fixed code is returned as a string.
}
\details{
Pass a \code{\link[=flowr_lint]{flowr_lint()}} result as \code{code} to reuse it directly --- no
re-analysis, so applying fixes right after linting is instant.
}
\section{Analysing the current project}{

Every command that takes \code{code}/\code{file}/\code{folder} follows the same rule: pass
\code{code} (a string), \code{file} (one \code{.R} file) or \code{folder} (analysed recursively),
and with \strong{none} of them the command analyses the \strong{current R project}.
flowr walks up from the working directory to the \emph{outermost} project root ---
a \code{DESCRIPTION} or \code{.Rproj}, else the enclosing git root, else the working
directory if it holds \code{.R} files --- analyses that project's \code{.R} files, and
reports the root it chose (unless \code{options(flowr.quiet = TRUE)}). If nothing
looks like a project, the command errors asking for \code{code}, \code{file} or
\code{folder}.
}

\examples{
\dontrun{
flowr_lint_fix("x = 1")                       # -> "x <- 1" (returned)
flowr_lint_fix(file = "analysis.R")           # rewrites the file in place
cat(flowr_lint_fix(file = "analysis.R", preview = TRUE))  # dry run

l <- flowr_lint(file = "analysis.R")          # lint once ...
flowr_lint_fix(l)                              # ... then fix instantly (reused)
}
}
\seealso{
\code{\link[=flowr_lint]{flowr_lint()}}
}
