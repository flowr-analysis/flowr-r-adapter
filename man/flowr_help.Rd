% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/help.R
\name{flowr_help}
\alias{flowr_help}
\title{Look up a function or package the way \verb{?}/\code{help()} does, backed by flowR's signature database}
\usage{
flowr_help(
  topic = NULL,
  package = NULL,
  version = NULL,
  parameters = NULL,
  required_parameters = NULL,
  call_graph = FALSE,
  session = NULL
)
}
\arguments{
\item{topic}{A function name (optionally \code{"pkg::fn"}), or \code{NULL} with
\code{package} set for a package overview. Glob wildcards (\code{*}, \verb{?}) search.}

\item{package}{Restrict to (or, with no \code{topic}, summarize) this package.
Glob wildcards search package names.}

\item{version}{A version spec: an exact version, a glob (\code{"3.*"}), a semver
range (\code{">=3.0.0"}), or a release-date bound (\code{"<=2026"}, \code{">=2021.05"}).}

\item{parameters}{Keep only functions with a parameter matching every one of
these names (glob wildcards allowed, position-independent).}

\item{required_parameters}{Keep only functions with exactly this many
required (no-default) parameters, excluding \code{...}.}

\item{call_graph}{For a single resolved function, also compute its
transitive call graph as a mermaid.live link.}

\item{session}{A \code{flowr_session}; the active default session is used when
\code{NULL} (one is started on first use).}
}
\value{
A \code{flowr_help}, printed as a signature/package/match listing.
}
\description{
Resolves \code{topic} against flowR's signature database (see \emph{The signature
database} in \code{\link[=flowr_install]{flowr_install()}}) instead of the local library, so it works
for any CRAN package's exports whether or not that package is installed ---
the parameters and defaults of a function, whether it is exported/deprecated,
its S3 dispatch, source location, and CRAN-mirror/rdrr.io documentation
links.
}
\details{
An unqualified \code{topic} (e.g. \code{"filter"}) is looked up as a \strong{function}
across every loaded package, exactly like \code{help("filter")} searches the
attached search path --- pass \code{package} to see a package overview instead
(like \code{help(package = "dplyr")}), or qualify the topic (\code{"dplyr::filter"})
to go straight to one package's version. A topic found in exactly one
package resolves directly to its full signature; several hits are listed
(like \code{apropos()}) so you can narrow with \code{package}.

Needs a signature database installed (see \code{\link[=flowr_install]{flowr_install()}}); errors with
that hint if none is mounted.
}
\examples{
\dontrun{
flowr_install()                    # once, to get the signature database
flowr_help("dplyr::filter")        # a specific package's function
flowr_help("pivot_longer")         # bare topic: searched across packages
flowr_help(package = "ggplot2")    # package overview, like help(package=)
}
}
\seealso{
\code{\link[=flowr_install]{flowr_install()}}, \code{\link[=query]{query()}}
}
