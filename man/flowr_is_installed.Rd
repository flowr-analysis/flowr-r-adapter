% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install.R
\name{flowr_is_installed}
\alias{flowr_is_installed}
\title{Is a flowR engine installed?}
\usage{
flowr_is_installed(
  engine = c("any", "all", "binary", "bundled", "node", "docker"),
  version = flowr_option("flowr_version")
)
}
\arguments{
\item{engine}{One engine (\code{"binary"}, \code{"bundled"}, \code{"node"}, \code{"docker"}),
\code{"any"} (is at least one ready?), or \code{"all"} (a named logical for every
engine).}

\item{version}{flowR version to check.}
}
\value{
For a single engine or \code{"any"}, a single \code{TRUE}/\code{FALSE}. For \code{"all"},
a named logical vector, one entry per registered engine.
}
\description{
Reports engine readiness from the engine registry, so it stays correct as
engines are added. Use \code{"all"} to see every engine at once.
}
\details{
Note that \code{flowr_is_installed("any")} (the default) is \code{TRUE} whenever \emph{any}
engine is ready, including the \code{bundled} flowR that ships inside the package
(which only needs Node.js on your PATH). So it can report \code{TRUE} even when no
self-contained binary has been downloaded; in that bundled-only case it also
emits a one-line note pointing at \code{\link[=flowr_install]{flowr_install()}}. Ask specifically with
\code{flowr_is_installed("binary")} if you mean the downloaded binary.
}
\examples{
flowr_is_installed("binary")   # is the downloaded binary ready?
flowr_is_installed("all")      # readiness of every engine
flowr_is_installed()           # is any engine ready at all?
}
\seealso{
\code{\link[=flowr_install]{flowr_install()}}, \code{\link[=flowr_status]{flowr_status()}}
}
