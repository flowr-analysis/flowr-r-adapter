% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lint.R
\name{flowr_lint}
\alias{flowr_lint}
\title{Lint R code with flowR}
\usage{
flowr_lint(
  code = NULL,
  file = NULL,
  folder = NULL,
  session = NULL,
  rules = NULL,
  format = c("data.frame", "jarl", "sarif", "github")
)
}
\arguments{
\item{code}{A string of R source code. Provide \code{code}, \code{file} or \code{folder};
with none, the current R project is analysed (see the \emph{Analysing the
current project} section).}

\item{file}{Path to a single R file to analyse.}

\item{folder}{Path to a folder; all \code{.R} files in it (recursively) are
analysed together for a multi-file slice. Needs a real engine.}

\item{session}{A \code{flowr_session} (from \code{\link[=flowr_connect]{flowr_connect()}}); the central default
session is used when \code{NULL}. Pass an explicit one to work with several at once.}

\item{rules}{Which linter rules to run, as a character vector of rule names
(e.g. \code{c("absolute-file-paths", "unused-definitions")}). \code{NULL} (default)
uses the active set from \code{options(flowr.lint_rules = )} /
\code{\link[=flowr_set_config]{flowr_set_config()}}, which itself defaults to flowR's full rule set --- the
same rules the flowR VS Code extension enables. See the \emph{Linting} section of
\code{vignette("flowr-engines")} for the rule list and configuration.}

\item{format}{Output format: \code{"data.frame"} (default, lintr-compatible),
\code{"jarl"} (jarl's native JSON), \code{"sarif"} (SARIF 2.1.0), or \code{"github"}
(GitHub Actions annotation lines).}
}
\value{
For \code{format = "data.frame"}, a \code{flowr_lints} data frame (one row per
finding; the full flowR response kept in its \code{raw} attribute; printing shows
a compact, grouped report). For the other formats, the serialized output (a
JSON string, or a character vector of annotation lines).
}
\description{
Runs flowR's linter and returns its findings. By default (\code{format = "data.frame"}) the result is a tidy data frame with the same column names as
\code{lintr} (\code{filename}, \code{line_number}, \code{column_number}, \code{type}, \code{message},
\code{line}, \code{linter}) plus flowR-specific \code{certainty} and \code{fixable} columns, so it
drops straight into lintr-based reporting. The other formats emit exactly what
\href{https://jarl.etiennebacher.com}{jarl} produces --- its native \code{"jarl"} JSON,
\code{"sarif"} (SARIF 2.1.0), or \code{"github"} Actions annotations --- so flowr's
output is interchangeable with jarl's in CI and editors. flowR's linter covers
issues such as absolute file paths, unused definitions, naming conventions,
deprecated and network functions, unseeded randomness, dead code and possible
leaked credentials.
}
\details{
With no \code{code}/\code{file}/\code{folder}, lints the current R project (the working
directory's package or \code{.R} files), like the other flowr commands.
}
\section{Analysing the current project}{

Every command that takes \code{code}/\code{file}/\code{folder} follows the same rule: pass
\code{code} (a string), \code{file} (one \code{.R} file) or \code{folder} (analysed recursively),
and with \strong{none} of them the command analyses the \strong{current R project}.
flowr walks up from the working directory to the \emph{outermost} project root ---
a \code{DESCRIPTION} or \code{.Rproj}, else the enclosing git root, else the working
directory if it holds \code{.R} files --- analyses that project's \code{.R} files, and
reports the root it chose (unless \code{options(flowr.quiet = TRUE)}). If nothing
looks like a project, the command errors asking for \code{code}, \code{file} or
\code{folder}.
}

\examples{
\dontrun{
code <- "setwd('/tmp/x')\nunused_var <- 1"
flowr_lint(code)                 # lintr-compatible data frame
flowr_lint()                     # the current project
subset(flowr_lint(code), type == "warning")

# pick which rules run (per call, or as a persistent default)
flowr_lint(code, rules = c("absolute-file-paths", "unused-definitions"))
flowr_set_config(lint_rules = c("absolute-file-paths", "seeded-randomness"))

# jarl-compatible machine-readable output for CI
flowr_lint(code, format = "sarif")
cat(flowr_lint(code, format = "github"), sep = "\n")
}
}
\seealso{
\code{\link[=query]{query()}}, \code{\link[=slice]{slice()}}, \code{\link[=inspect_project]{inspect_project()}}
}
