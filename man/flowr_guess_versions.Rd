% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/guess.R
\name{flowr_guess_versions}
\alias{flowr_guess_versions}
\title{Guess the version range a project's dependencies must satisfy}
\usage{
flowr_guess_versions(
  code = NULL,
  file = NULL,
  folder = NULL,
  packages = NULL,
  date = NULL,
  clean = FALSE,
  disable_evidence = NULL,
  max_candidates = NULL,
  max_iterations = NULL,
  explode = FALSE,
  explode_order = c("newest", "oldest"),
  explode_prefer = NULL,
  explode_limit = NULL,
  session = NULL
)
}
\arguments{
\item{code}{A string of R source code. Provide \code{code}, \code{file} or \code{folder};
with none, the current R project is analysed (see the \emph{Analysing the
current project} section).}

\item{file}{Path to a single R file to analyse.}

\item{folder}{Path to a folder; all \code{.R} files in it (recursively) are
analysed together for a multi-file slice. Needs a real engine.}

\item{packages}{Restrict the guess to these packages; omit to guess for
every declared and used dependency.}

\item{date}{Only consider versions released on or before this day
(\code{"YYYY.MM.DD"}, also \code{"YYYY"} or \code{"YYYY.MM"}).}

\item{clean}{Ignore the project's declared constraints (\code{DESCRIPTION}
ranges, lockfile pins, transitive requirements); guess purely from code
usage and the date/R bounds. Sugar for \code{disable_evidence = c("declared", "transitive")}.}

\item{disable_evidence}{Exclude these evidence sources from consideration
entirely (flowR >= 2.13.1): any of \code{"declared"}, \code{"transitive"},
\code{"signature"}, \code{"date"}, \code{"base-r"}, \code{"available"}, \code{"indirect"}.}

\item{max_candidates}{Cap the number of candidate versions listed per
dependency in the result (flowR default 16).}

\item{max_iterations}{Bound both fixpoint loops (mutual transitive
refinement and arc consistency).}

\item{explode}{Also explode the guessed space into concrete per-dependency
version assignments (see \verb{$assignments} in the return value).}

\item{explode_order}{With \code{explode}, iterate each dependency's versions
newest-first (default) or oldest-first.}

\item{explode_prefer}{With \code{explode}, a named character vector of a version
to prefer per dependency when it survives the constraints, e.g.
\code{c(dplyr = "1.1.4")}.}

\item{explode_limit}{With \code{explode}, cap the number of assignments produced.}

\item{session}{A \code{flowr_session} (from \code{\link[=flowr_connect]{flowr_connect()}}); the central default
session is used when \code{NULL}. Pass an explicit one to work with several at once.}
}
\value{
A \code{flowr_guess}: \verb{$dependencies} (a data frame, one row per
dependency, with list-columns \code{candidates}, \code{declared_constraints},
\code{linked_with} and \code{evidence}), plus \verb{$date_cutoff}, \verb{$r_version},
\verb{$runnable_combinations}/\verb{$possible_combinations}, \verb{$linked_groups},
\verb{$assignments} (with \code{explode}) and \verb{$message}.
}
\description{
Wraps flowR's \code{guess-dep-versions} query: for every dependency a project
declares or calls, combines the constraints it \emph{declares} (a \code{DESCRIPTION}
range, an \code{rproject.toml}/lockfile pin, and their transitive requirements)
with what the code actually \emph{does} (which package functions it calls and
with which arguments, matched against the signature database --- a named
argument a function only gained in some version raises the lower bound).
An optional \code{date} caps every guess to releases available at that point in
time, and base-R packages are additionally bounded by the assumed/declared
R version.
}
\details{
Needs a signature database installed (see \code{\link[=flowr_install]{flowr_install()}}); without one,
the result carries no dependencies and \verb{$message} explains why. Usage-based
evidence also needs the \code{"history"} set specifically (\code{"current"} only
carries the latest version of each package): missing it, an interactive
session is offered the install once; others get a one-line note instead.
}
\section{Analysing the current project}{

Every command that takes \code{code}/\code{file}/\code{folder} follows the same rule: pass
\code{code} (a string), \code{file} (one \code{.R} file) or \code{folder} (analysed recursively),
and with \strong{none} of them the command analyses the \strong{current R project}.
flowr walks up from the working directory to the \emph{outermost} project root ---
a \code{DESCRIPTION} or \code{.Rproj}, else the enclosing git root, else the working
directory if it holds \code{.R} files --- analyses that project's \code{.R} files, and
reports the root it chose (unless \code{options(flowr.quiet = TRUE)}). If nothing
looks like a project, the command errors asking for \code{code}, \code{file} or
\code{folder}.
}

\examples{
\dontrun{
flowr_install()          # once, to get the signature database
flowr_guess_versions()   # the R package/project in the working directory
flowr_guess_versions(packages = "dplyr", date = "2023.01")
flowr_guess_versions(disable_evidence = "signature")   # declared/usage only
}
}
\seealso{
\code{\link[=flowr_install]{flowr_install()}}, \code{\link[=query]{query()}}
}
