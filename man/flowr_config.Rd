% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{flowr_config}
\alias{flowr_config}
\title{Inspect the effective flowr configuration}
\usage{
flowr_config()
}
\value{
A named list (class \code{flowr_config}) with every configuration value as
currently resolved. Printing groups the values and marks the server-only
ones (which apply only when flowr starts or connects to a flowR server) and
the live transport. Assigning into it (\code{cfg$name <- value}) sets the option.
}
\description{
Resolves every configuration knob to its currently effective value. Each knob
\verb{<name>} is resolved, in decreasing precedence, from: a function argument, the
R option \verb{options(flowr.<name> = )}, the environment variable \verb{FLOWR_<NAME>},
the JSON config file (\code{\link[=flowr_config_file]{flowr_config_file()}}), then the package default.
}
\details{
Any of those set a value and are fully supported. \verb{options(flowr.<name> = )}
is perfectly fine for interactive use; \code{\link[=flowr_set_config]{flowr_set_config()}} is a convenience
that does it for you (and can persist), and you may also assign into the
returned object (\code{cfg$engine <- "binary"}), which sets the option.
}
\examples{
flowr_config()
flowr_config()$engine
\dontrun{
cfg <- flowr_config()
cfg$engine <- "binary"          # same as options(flowr.engine = "binary")
options(flowr.request_timeout = 300)
}
}
\seealso{
\code{\link[=flowr_set_config]{flowr_set_config()}}, \code{\link[=flowr_status]{flowr_status()}}
}
