% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install.R
\name{flowr_update}
\alias{flowr_update}
\title{Update flowR, or check for a newer version}
\usage{
flowr_update(
  version = NULL,
  engine = c("binary", "node", "docker"),
  quiet = FALSE
)
}
\arguments{
\item{version}{flowR version to switch to, e.g. \code{"2.12.0"}. \code{NULL} (default)
only checks for a newer version.}

\item{engine}{Engine to install and use for it: \code{"binary"}, \code{"node"} or
\code{"docker"}.}

\item{quiet}{Suppress progress messages.}
}
\value{
The version string: the latest available when only checking, or the
newly targeted version when switching. Returned invisibly.
}
\description{
With no \code{version}, checks the flowR release feed and reports whether a newer
version than the one you currently target is available (it does not download
anything). With an explicit \code{version}, switches to that release for
subsequent sessions and installs its engine. The shipped \code{bundled} engine is
pinned to the packaged flowR, so installs use the downloadable \code{binary}
(default) or the \code{node} engine.
}
\examples{
\dontrun{
flowr_update()                   # is there a newer flowR?
flowr_update("2.12.0")           # switch to a newer flowR
flowr_update("2.10.0", "node")  # or an older one, via Node
}
}
\seealso{
\code{\link[=flowr_install]{flowr_install()}}, \code{\link[=flowr_status]{flowr_status()}}
}
