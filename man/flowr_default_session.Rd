% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/session.R
\name{flowr_default_session}
\alias{flowr_default_session}
\title{The central flowR session}
\usage{
flowr_default_session(...)
}
\arguments{
\item{...}{Passed to \code{\link[=flowr_connect]{flowr_connect()}} when the session is first created.}
}
\value{
A \code{flowr_session}.
}
\description{
Returns the single shared session, creating one on first use. The high-level
helpers all use it, so \verb{library(flowr); slice(code, criterion)} works with no
setup. Call \code{\link[=flowr_connect]{flowr_connect()}} to (re)create it with a specific engine.
}
\examples{
\dontrun{
s <- flowr_default_session()     # the shared session (created on first use)
is_flowr_session(s)
}
}
