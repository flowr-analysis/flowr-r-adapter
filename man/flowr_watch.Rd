% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{flowr_watch}
\alias{flowr_watch}
\title{Re-run a flowr command whenever a file changes}
\usage{
flowr_watch(
  file,
  action = function(f) print(flowr_overview(file = f)),
  interval = 0.5,
  clear = TRUE
)
}
\arguments{
\item{file}{Path to the R file to watch.}

\item{action}{A function of one argument (the file path), run on every change.
Defaults to printing \code{\link[=flowr_overview]{flowr_overview()}} of the file. For slicing, pass e.g.
\code{function(f) print(slice(file = f, criterion = "5@x"))}.}

\item{interval}{Polling interval in seconds.}

\item{clear}{Clear the terminal before each refresh so only the latest output
is shown (like \code{watch(1)}). \code{TRUE} by default on an ANSI-capable terminal;
set \code{FALSE} to keep every refresh in the scrollback. Ignored where ANSI is
not available (then refreshes are just separated by a blank line).}
}
\value{
Does not return normally; runs until interrupted.
}
\description{
Watches \code{file} and re-runs \code{action} each time its contents change, printing
the result, until you interrupt it (Ctrl-C / Esc). This brings flowR's
\verb{watch://} REPL input to the R API for any command. In flowR's own console
(\code{\link[=flowr_console]{flowr_console()}}) you can equivalently pass \verb{watch://<path>} in place of
\verb{file://<path>} to a \code{:}-command (e.g. \verb{:slice watch://script.R}).
}
\examples{
\dontrun{
flowr_watch("script.R")   # re-print the overview on every save
flowr_watch("script.R", function(f) print(slice(file = f, criterion = "8@result")))
}
}
\seealso{
\code{\link[=flowr_console]{flowr_console()}}, \code{\link[=slice]{slice()}}, \code{\link[=flowr_overview]{flowr_overview()}}
}
