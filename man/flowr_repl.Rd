% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{flowr_repl}
\alias{flowr_repl}
\title{Execute a flowR REPL command, or drop into an interactive REPL}
\usage{
flowr_repl(
  expression = NULL,
  ansi = FALSE,
  session = NULL,
  r_access = TRUE,
  native = TRUE
)
}
\arguments{
\item{expression}{The REPL expression/command to run, or \code{NULL} to start an
interactive REPL.}

\item{ansi}{Keep ANSI colour codes in the output (default \code{FALSE}).}

\item{session}{A \code{flowr_session}; the central default is used when \code{NULL}.}

\item{r_access}{For the interactive REPL, allow running R code you type (via
the r-shell engine and \code{--r-session-access}). On by default since you drive
the REPL; set \code{FALSE} for a flowR-only shell.}

\item{native}{For the interactive REPL, use flowR's own console (history and
tab completion). \code{FALSE} uses the socket-relayed line loop over the current
session (the "messaging" REPL), which streams each response through flowr.}
}
\value{
For a single command, a \code{flowr_repl} with the combined \code{output} and
the raw \code{messages}; for the interactive REPL, \code{invisible(NULL)}.
}
\description{
With an \code{expression}, runs a single REPL expression or command (commands are
prefixed with \code{:}, e.g. \code{":help"}) and returns its streamed output.
With no \code{expression} in an interactive session, opens flowR's own REPL with
native history (up-arrow) and tab completion via \code{\link[=flowr_console]{flowr_console()}}. Set
\code{native = FALSE} to use the socket-relayed line loop over the current session
instead. Type R code or \code{:}-commands until you enter \verb{:quit}.
}
\examples{
\dontrun{
flowr_repl(":help")           # one command
flowr_repl()                   # interactive native REPL (R execution on)
flowr_repl(native = FALSE)     # socket-relayed line loop instead
}
}
