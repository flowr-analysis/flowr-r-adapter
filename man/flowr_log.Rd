% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/status.R
\name{flowr_log}
\alias{flowr_log}
\title{Read the flowR server's log}
\usage{
flowr_log(session = NULL, n = 200L)
}
\arguments{
\item{session}{A \code{flowr_session}; the active default session when \code{NULL}.}

\item{n}{Number of trailing lines to return (\code{Inf} for the whole log).}
}
\value{
A \code{flowr_log} (a character vector of log lines); its \code{file} attribute
is the log path. Printing shows the lines.
}
\description{
Returns the log output (stdout and stderr) of the flowR server backing a
session, mostly for debugging. For detailed flowR logging, start the server
with \code{flowr_set_config(verbose = TRUE)} (or \code{options(flowr.verbose = TRUE)})
before the first call, then read it here.
}
\examples{
\dontrun{
flowr_set_config(verbose = TRUE)
slice("x <- 1\ncat(x)", "2@x")
flowr_log()
}
}
\seealso{
\code{\link[=flowr_status]{flowr_status()}}, \code{\link[=flowr_config]{flowr_config()}}
}
