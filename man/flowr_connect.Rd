% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/session.R
\name{flowr_connect}
\alias{flowr_connect}
\title{Start a flowR engine and connect to it}
\usage{
flowr_connect(
  engine = flowr_option("engine"),
  flowr_version = flowr_option("flowr_version"),
  flowr_engine = flowr_option("flowr_engine"),
  host = flowr_option("host"),
  port = flowr_option("port"),
  ws = flowr_option("ws"),
  quiet = flowr_option("quiet")
)
}
\arguments{
\item{engine}{Which engine to use: \code{"auto"} (default), \code{"bundled"} (the flowR
JS+wasm shipped with the package, run on your Node), \code{"binary"}
(self-contained, no Node), \code{"node"} or \code{"docker"}.}

\item{flowr_version}{flowR version to obtain (default \code{"2.13.1"}).}

\item{flowr_engine}{flowR parser engine, \code{"tree-sitter"} (default, needs no R)
or \code{"r-shell"} (reuses the R on your \code{PATH}).}

\item{host, port}{Bind host (loopback) and preferred port for the spawned
server; a free port is chosen automatically if the preferred one is taken.}

\item{ws}{Use a WebSocket transport instead of raw TCP.}

\item{quiet}{Suppress progress messages.}
}
\value{
An object of class \code{flowr_session}.
}
\description{
Obtains a running flowR server through the selected engine and opens a client
connection to it.  The returned session is passed to \code{\link[=slice]{slice()}}, \code{\link[=query]{query()}},
\code{\link[=flowr_repl]{flowr_repl()}}, etc., or left implicit (they use a lazily created default
session when none is supplied).
}
\examples{
\dontrun{
s <- flowr_connect()
slice("x <- 1\ny <- 2\nprint(x)", "3@x")
flowr_disconnect(s)
}
}
\seealso{
\code{\link[=flowr_disconnect]{flowr_disconnect()}}, \code{\link[=slice]{slice()}}, \code{\link[=query]{query()}}
}
