% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{slice}
\alias{slice}
\title{Slice R code with flowR}
\usage{
slice(
  code = NULL,
  criterion,
  direction = c("backward", "forward"),
  file = NULL,
  folder = NULL,
  cfg = FALSE,
  style = getOption("flowr.slice_style", "diff"),
  session = NULL
)
}
\arguments{
\item{code}{A string of R source code. Provide this, \code{file} or \code{folder}.}

\item{criterion}{Slicing criteria, a character vector such as \code{"3@x"}
(line 3, variable \code{x}), \code{"7:3"} (line:column) or \code{"$42"} (a node id).}

\item{direction}{Slice \code{"backward"} (default: everything the criterion
depends on) or \code{"forward"} (everything that depends on the criterion).}

\item{file}{Path to a single R file to analyse.}

\item{folder}{Path to a folder; all \code{.R} files in it (recursively) are
analysed together for a multi-file slice. Needs a real engine.}

\item{cfg}{Also request the control-flow graph in the underlying analysis.}

\item{style}{Default print style for the returned slice: \code{"diff"}, \code{"gray"}
or \code{"code"}. See \code{\link[=print.flowr_slice]{print.flowr_slice()}}.}

\item{session}{A \code{flowr_session} (from \code{\link[=flowr_connect]{flowr_connect()}}); the central default
session is used when \code{NULL}. Pass an explicit one to work with several at once.}
}
\value{
A \code{flowr_slice} with \code{code} (reconstructed slice), \code{ids} (included
node ids), \code{original} (the input source), \code{lines} (original line numbers in
the slice), plus \code{criteria}, \code{direction} and the raw \code{response}. Printing
shows a colored git-style diff by default; see \code{\link[=print.flowr_slice]{print.flowr_slice()}}.
}
\description{
Computes a program slice for one or more slicing criteria and returns the
reconstructed, sliced code together with the ids of the included nodes.
}
\examples{
\dontrun{
flowr_install()   # once, to get a flowR engine
slice("x <- 1\ny <- 2\ncat(x)", "3@x")$code
}
}
\seealso{
\code{\link[=query]{query()}}, \code{\link[=flowr_locations]{flowr_locations()}}
}
