% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{slice}
\alias{slice}
\title{Slice R code with flowR}
\usage{
slice(
  code = NULL,
  criterion,
  direction = c("backward", "forward"),
  include_callees = FALSE,
  inline_sources = FALSE,
  file = NULL,
  folder = NULL,
  cfg = FALSE,
  style = getOption("flowr.slice_style", "diff"),
  session = NULL
)
}
\arguments{
\item{code}{A string of R source code. Provide \code{code}, \code{file} or \code{folder};
with none, the current R project is analysed (see the \emph{Analysing the
current project} section).}

\item{criterion}{Slicing criteria, a character vector such as \code{"3@x"}
(line 3, variable \code{x}), \code{"7:3"} (line:column) or \code{"$42"} (a node id).}

\item{direction}{Slice \code{"backward"} (default: everything the criterion
depends on) or \code{"forward"} (everything that depends on the criterion).}

\item{include_callees}{Continue a \strong{backward} slice past a function-definition
boundary, also pulling in the definition's binding and call sites. flowR
ignores this for forward slices, so \code{slice()} warns if you combine the two.
Off by default; needs flowR >= 2.12.}

\item{inline_sources}{Inline resolvable \code{source()} calls into the reconstructed
code, so the slice is a single self-contained R script. \code{source()} calls that
are cyclic or whose path cannot be resolved are kept verbatim and reported in
the result's \code{inline_warnings} (and warned about once). Off by default; needs
flowR >= 2.12.}

\item{file}{Path to a single R file to analyse.}

\item{folder}{Path to a folder; all \code{.R} files in it (recursively) are
analysed together for a multi-file slice. Needs a real engine.}

\item{cfg}{Also request the control-flow graph in the underlying analysis.}

\item{style}{Default print style for the returned slice: \code{"diff"}, \code{"gray"}
or \code{"code"}. See \code{\link[=print.flowr_slice]{print.flowr_slice()}}.}

\item{session}{A \code{flowr_session} (from \code{\link[=flowr_connect]{flowr_connect()}}); the central default
session is used when \code{NULL}. Pass an explicit one to work with several at once.}
}
\value{
A \code{flowr_slice} with \code{code} (reconstructed slice), \code{ids} (included
node ids), \code{original} (the input source), \code{lines} (original line numbers in
the slice), plus \code{criteria}, \code{direction} and the raw \code{response}. With
\code{inline_sources = TRUE} it also carries \code{inline_warnings}, a data frame of
\code{source()} calls that could not be inlined (\code{kind} is \code{"cycle"} or
\code{"unresolved"}, with the call's node \code{id} and, when known, its \code{path}).
Printing shows a colored git-style diff by default; see \code{\link[=print.flowr_slice]{print.flowr_slice()}}.
}
\description{
Computes a program slice for one or more slicing criteria and returns the
reconstructed, sliced code together with the ids of the included nodes.
}
\section{Analysing the current project}{

Every command that takes \code{code}/\code{file}/\code{folder} follows the same rule: pass
\code{code} (a string), \code{file} (one \code{.R} file) or \code{folder} (analysed recursively),
and with \strong{none} of them the command analyses the \strong{current R project}.
flowr walks up from the working directory to the \emph{outermost} project root ---
a \code{DESCRIPTION} or \code{.Rproj}, else the enclosing git root, else the working
directory if it holds \code{.R} files --- analyses that project's \code{.R} files, and
reports the root it chose (unless \code{options(flowr.quiet = TRUE)}). If nothing
looks like a project, the command errors asking for \code{code}, \code{file} or
\code{folder}.
}

\examples{
\dontrun{
flowr_install()   # once, to get a flowR engine
slice("x <- 1\ny <- 2\ncat(x)", "3@x")$code
}
}
\seealso{
\code{\link[=query]{query()}}, \code{\link[=flowr_locations]{flowr_locations()}}
}
