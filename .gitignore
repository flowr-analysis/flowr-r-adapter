.Rproj.user
.Rhistory
.RData
.Ruserdata
flowr-signing-key.pem
signed-build/
inst/flowr-manifest.json.orig
# rv installs the locked dependencies here; keep only rproject.toml + rv.lock
rv/
# build / signing artifacts
*.tar.gz
*.tar.gz.sig
*.tar.gz.sha256
flowr.Rcheck/
# local rv activation (rv sync regenerates it); must not reach CI, where it would
# point R at a missing rv library
.Rprofile
# npm install target for rebuilding inst/flowr-js (see MAINTAINING.md); never committed
node_modules/
package-lock.json
# make sigdb's shard cache (tools/pack-sigdb.sh); never committed
.sigdb-work/
